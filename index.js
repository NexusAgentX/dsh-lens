/**
 * DeepSeek Harness plugin entry.
 *
 * 0.0.1 is a reserved npm name / installable bundle stub.
 * The host-native lens pipeline lands in a later release.
 */

export const name = 'dsh-lens'

export function apply() {
  console.info('[dsh-lens] placeholder 0.0.1 loaded — write/edit feedback is not implemented yet')
}
